{
  "name": "@tokeninsights/build-tools",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check:schema": "node src/check-schema.ts",
    "check:web": "node src/check-web.ts",
    "setup-dev-data": "node src/setup-dev-data.ts",
    "generate:homebrew": "node src/generate-homebrew-formula.ts",
    "test": "node --test test/*.test.ts",
    "typecheck": "tsc --project tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "26.4.1",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=26"
  }
}
