# Build tools

Private Node tooling for repository builds, schema validation, tests, releases, and fixture-backed development.

Production does not depend on this package. The Go binary embeds prebuilt browser assets and runs without Node, npm, pnpm, or `node_modules`.

Run these tools through the root package scripts.
