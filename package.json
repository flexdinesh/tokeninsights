{
  "name": "tokeninsights",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=25"
  },
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "check-schema": "pnpm --filter check-schema run check-schema",
    "build:logger": "pnpm --filter @tokeninsights/logger run build",
    "build:opencode-server": "pnpm --filter @tokeninsights/opencode-server build",
    "vendor:opencode-server": "pnpm --filter @tokeninsights/opencode-server run vendor",
    "build:pi": "pnpm --filter @tokeninsights/pi build",
    "build:plugins": "pnpm run build:opencode-server && pnpm run build:pi && pnpm run vendor:opencode-server",
    "build:cli": "pnpm --filter tokeninsights-cli run build",
    "install:cli": "cd packages/cli && go install ./cmd/tokeninsights-cli",
    "build": "pnpm run build:logger && pnpm run build:plugins && pnpm run build:cli",
    "test": "pnpm --filter tokeninsights-cli run test"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "packageManager": "pnpm@11.4.0+sha512.f0febc7e37552ab485494a914241b338e0b3580b93d54ce31f00933015880863129038a1b4ae4e414a0ee63ac35bf21197e990172c4a68256450b5636310968f"
}
