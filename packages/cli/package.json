{
  "name": "@tokeninsights/cli",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "go build -o bin/tokeninsights ./cmd/tokeninsights",
    "lint": "go tool -modfile=golangci-lint.mod golangci-lint run ./...",
    "test": "go test ./..."
  }
}
